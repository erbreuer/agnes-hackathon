F07 — Refine loop + POST /refine
Goal: Iterate on user feedback without re-analyzing the room.
Depends on: F06.
Files to create / modify
•	backend/main.py
Requirements
25.	POST /refine accepts {session_id, feedback (str)}.
26.	Load the stored space_brief for that session, re-run plan_design(feedback=...) -> scout_products -> render_room, update the session, and return the same payload shape as /design.
27.	Return HTTP 404 if the session_id is unknown.
Implementation notes
This is the 'loop till satisfied' mechanism: only steps 2–4 of the pipeline re-run; the room analysis is reused.
Validation
curl -X POST http://localhost:8000/refine -H "Content-Type: application/json" -d '{"session_id":"<id>","feedback":"cheaper sofa, more plants"}'
# expect new renders + products for the same room
Done when: Refine returns updated results using the stored brief.
